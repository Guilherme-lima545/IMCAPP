import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxTitle: {
        alignItems: "center",
        padding: 10,

    },
    TextTitle: {
        color: "#ff0043",
        fontSize: 24,
        fontWeight: "bold",
    }
})

export default styles